<%= form_for(@second_rails_app) do |f| %>
  <% if @second_rails_app.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@second_rails_app.errors.count, "error") %> prohibited this second_rails_app from being saved:</h2>

      <ul>
      <% @second_rails_app.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
