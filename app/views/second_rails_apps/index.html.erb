<h1>Listing second_rails_apps</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @second_rails_apps.each do |second_rails_app| %>
  <tr>
    <td><%= link_to 'Show', second_rails_app %></td>
    <td><%= link_to 'Edit', edit_second_rails_app_path(second_rails_app) %></td>
    <td><%= link_to 'Destroy', second_rails_app, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Second rails app', new_second_rails_app_path %>
